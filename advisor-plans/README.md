# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `07d5dee`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the assigned plan fully before starting, honor its STOP conditions, and
update the status row when done.

These plans are stored in `advisor-plans/` because the repository already had a
pre-existing unrelated `plans/postgres-kysely-migration-plan.md` and no
`plans/README.md` index.

## Execution Order & Status

| Plan | Title                                          | Priority | Effort | Depends on      | Status |
| ---- | ---------------------------------------------- | -------- | ------ | --------------- | ------ |
| 001  | Fix JWT expiration units                       | P1       | S      | -               | TODO   |
| 002  | Run real Playwright tests in CI                | P1       | S      | -               | TODO   |
| 003  | Constrain NoSketch proxy targets               | P1       | M      | 002 recommended | DONE   |
| 004  | Replace or harden XLSX export path             | P1       | M      | 002 recommended | TODO   |
| 005  | Preserve saved facets in visualization queries | P1       | S      | 002 recommended | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency Notes

- Plans 003-005 can be implemented independently, but plan 002 should land
  first if this work is going through CI, because the current workflow installs
  Playwright and then runs `pnpm run test`, which is only `exit 0`.
- Plan 003 should include API or server-level tests; if plan 002 has not landed,
  run those tests locally and call out that CI will not yet enforce them.
- Plan 004 changes a runtime dependency and a server endpoint; keep it isolated
  from unrelated export UX work.
- Plan 005 is intentionally narrow. Do not fold broader visualization query
  routing or publishing cache fixes into it.

## Findings Considered And Rejected

- Dev-only transitive audit advisories through Nuxt devtools were not promoted
  above the direct runtime `xlsx` finding because the vulnerable direct package
  is reachable from a server endpoint.
- Full visualization ownership redesign was deferred because it is larger and
  riskier than the first five findings selected here.
- Broad pagination/list-response work was deferred because it is a performance
  improvement, not a blocker for the selected correctness/security issues.
